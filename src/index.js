export { mutex } from "./mutex.js"
export { semaphore } from "./semaphore.js"

/**
 * @typedef {import('./mutex.js').RedisOptions} RedisOptions
 * @typedef {import('./mutex.js').MutexOptions} MutexOptions
 * @typedef {import('./mutex.js').AcquireOptions} AcquireOptions
 * @typedef {import('./mutex.js').AcquireResult} AcquireResult
 * @typedef {import('./mutex.js').PeekResult} PeekResult
 * @typedef {import('./mutex.js').MutexLockInfo} MutexLockInfo
 * @typedef {import('./semaphore.js').SemaphoreOptions} SemaphoreOptions
 * @typedef {import('./semaphore.js').SemaphoreAcquireOptions} SemaphoreAcquireOptions
 * @typedef {import('./semaphore.js').SemaphorePeekResult} SemaphorePeekResult
 * @typedef {import('./semaphore.js').SemaphoreLockInfo} SemaphoreLockInfo
 */
